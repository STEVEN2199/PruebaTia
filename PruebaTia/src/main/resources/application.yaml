spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pruebatia?createDatabaseIfNotExist=true&autoReconnect=true&serverTimezone=UTC
    username: dantezz # Reemplaza con tu usuario de MySQL
    password: natasha2150_ # Reemplaza con tu contraseña de MySQL
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html